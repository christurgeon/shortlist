# Sample only — not installed automatically. See README.md.
[Unit]
Description=Daily shortlist snapshot capture

[Timer]
OnCalendar=*-*-* 21:30:00 UTC
Persistent=true

[Install]
WantedBy=timers.target
