# Secrets — never commit real API keys
.env

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/

# Snapshot output / screen results (anchored to top level so they don't match
# the src/shortlist/research package dir)
/snapshots/
*.csv
# commit the template; portfolio.csv stays ignored by *.csv above
!portfolio.example.csv
/research/

# Scratch: pulled raw data samples (data-source exploration)
/scratch/
.cache/

# Backtest caches (SEC companyfacts + ticker map, warmed by shortlist-backtest --source xbrl)
/.xbrl_cache/

# Scout artifacts (daily manifests, briefs, state)
/scout/
state/scout_state.json

# Prospect skill artifacts (weekly idea-hunt briefs)
/prospect/

# Superpowers process artifacts (design specs + implementation plans) — kept local, not published
/docs/superpowers/specs/
/docs/superpowers/plans/
